{
  "name": "puhehlinluettelo-osa3",
  "version": "1.0.0",
  "description": "osan 3 tehtäviä",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../osa2/puhelinluettelo/ && npm run build && cp -r dist ../../osa3/puhelinluettelo-osa3/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Eevert Salminen",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.17.1",
    "morgan": "^1.10.1"
  }
}
